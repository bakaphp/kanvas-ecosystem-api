apiVersion: autoscaling/v2
kind: VerticalPodAutoscaler
metadata:
  name: {{.Values.deployments.queue.name}}-vpa
spec:
  targetRef:
  apiVersion: "apps/v1"
  kind: Deployment
    name: {{.Values.deployments.queue.name}}
  updatePolicy:
  updateMode: "Auto"