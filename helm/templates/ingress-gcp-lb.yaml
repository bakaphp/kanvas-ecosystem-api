apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{.Values.services.loadBalancer.appName}}
  namespace: {{.Values.namespace}}
  annotations:
    kubernetes.io/ingress.regional-static-ip-name: kanvas-cluster-development-static-ip-address
    networking.gke.io/managed-certificates: managed-cert
    kubernetes.io/ingress.class: "gce"
spec:
  defaultBackend:
    service:
      name: {{.Values.services.loadBalancer.appName}}
      port:
        number: 80