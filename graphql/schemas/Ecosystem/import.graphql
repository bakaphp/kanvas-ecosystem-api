extend type Mutation @guard {
    importToEcosystem(filesystemId:ID, companyId: Int!, regionId: Int, mapperId: Int): String
        @field(
            resolver: "App\\GraphQL\\Ecosystem\\Mutations\\Import\\ImportManagementMutation@import"
        )
}