input ModuleConfigInput {
    key: String!
    value: Mixed!
    entity_uuid: String
    public: Boolean
    "Encrypt the value at rest. Reads decrypt transparently for internal use; admin queries return it masked (null)."
    secret: Boolean
}

type ModuleConfig {
    key: String!
    value: Mixed
    public: Boolean!
}
