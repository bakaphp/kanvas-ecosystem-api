
input ShopifySetupInput {
    client_id: String!
    client_secret: String!
    shop_name: String!
    region_id: ID!
    companies_id: ID
}

extend type Mutation @guard {
    shopifySetup(input: ShopifySetupInput!): Boolean
        @field(
            resolver: "App\\GraphQL\\Connector\\Shopify\\Mutations\\Shopify@shopifySetup"
        )
}
