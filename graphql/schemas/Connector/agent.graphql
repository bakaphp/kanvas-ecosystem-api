enum AgentProvider {
    OPENCLAW
    HERMES
}

extend type Mutation @guard {
    launchAgent(input: LaunchAgentInput!): AgentDeploymentType!
        @field(
            resolver: "App\\GraphQL\\Connector\\Agents\\Mutations\\AgentDeploymentMutation@launch"
        )
}
